<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Vendor Works Gallery Page</title>
    <link rel="stylesheet" href="/css/skill.css">
    <link href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <!-- Font Awesome for Stars -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css" rel="stylesheet">
    <script src="https://kit.fontawesome.com/56adb67a5b.js" crossorigin="anonymous"></script>
    <style>
        :root {
            --body-bg: rgba(127, 252, 212, 0.1);
            --text-bg: orange;
            --box1-bg: #b8860B;
            --box2-bg: #DC143c;
        }
        * {
            margin: 0;
            padding: 0;
            outline: none;
            box-sizing: border-box;
        }
        body {
            background-color: var(--body-bg) !important;
        }
        .skill-intro {
            margin-top: 5rem;
            padding: 40px;
            font-size: large;
            margin: 10px;
        }
        .skill-intro .wrap-intro-txt #h3 {
            color: var(--text-bg);
        }
        .skill-intro .wrap-intro-txt h2, #h3 {
            color: var(--text-bg);
        }
        .wlc-img {
            margin-top: 8rem;
            padding: 20px;
        }
        .wlc-img img {
            width: 60vw;
        }
        .btn-div {
            margin: 20px;
            padding: 10px;
            font-size: large;
            color: white;
        }
        .btn-div .btn {
            width: 50%;
            color: white;
            background-color: var(--box1-bg);
            padding: 10px;
            border-radius: 10px;
        }
        .row .reg-data {
            margin-left: 20rem;
        }
        .image-info .join {
            background-color: var(--box1-bg);
            padding: 5px;
            color: white;
            cursor: pointer;
            border-radius: 10px;
        }
        #cont {
            margin-left: 25%;
        }
        #cont input {
            padding: 10px;
        }
        #cont label {
            padding: 10px;
            margin: 10px;
            font-size: x-large;
        }
        .form .card-header {
            color: var(--box1-bg);
        }
        .form {
            background-color: var(--body-bg);
        }
        .form label, input {
            padding: 10px;
        }
        .form input {
            padding: 10px;
        }
        .form .btn {
            padding: 10px;
            width: 30%;
            background-color: var(--box1-bg);
            font-size: larger;
            color: white;
            cursor: pointer;
        }
        .container .box-image {
            height: 500px;
            width: 100%;
            overflow: scroll;
        }
        .container .box-image {
            display: grid;
            gap: 2rem;
            grid-template-columns: repeat(4, 1fr);
        }
        .container .box-image .boxes {
            width: 300px;
            height: 300px;
            background-color: #b8860B;
        }
        .image-cont {
            margin-right: 10rem;
        }
        .rating {
            display: flex;
            flex-direction: row-reverse;
            justify-content: center;
        }
        .rating input {
            display: none;
        }
        .rating label {
            font-size: 2rem;
            color: #ddd;
            cursor: pointer;
        }
        .rating input:checked ~ label {
            color: gold;
        }
        .rating input:hover ~ label,
        .rating label:hover ~ label {
            color: gold;
        }
    </style>
</head>
<body>
    <section class="container">
        <article class="skill-intro">
            <div class="wrap-intro-txt text-center">
                <h2 id="buy" style="padding: 5px;"></h2>
            </div>
            <div class="intro text-center">
                <div class="text-center">
                    Vendor uploaded passed and recent works
                    <h5 style="color: #b8860B;">Vendor full name:
                        <% if (vendorWorks && Array.isArray(vendorWorks) && vendorWorks.length > 0) { %>
                            <%= vendorWorks[0].name %> <!-- Display the name from the first item in the array -->
                        <% } else if (vendorWorks && typeof vendorWorks === 'object') { %>
                            <%= vendorWorks.name %> <!-- If it's a single object -->
                        <% } else { %>
                            No name available
                        <% } %>
                    </h5>
                </div>
            </div>
        </article>
    </section>

    <hr>
    <div class="modal fade" id="ratingModal" tabindex="-1" role="dialog" aria-labelledby="ratingModalLabel" aria-hidden="true">
        <div class="modal-dialog" role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="ratingModalLabel">Rate Us</h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body">
                    <form id="ratingForm">
                        <div class="form-group">
                            <label for="rating">Please select a rating:</label>
                            <div class="rating">
                                <input type="radio" name="star" id="star5" value="5">
                                <label for="star5"><i class="fas fa-star"></i></label>
                                <input type="radio" name="star" id="star4" value="4">
                                <label for="star4"><i class="fas fa-star"></i></label>
                                <input type="radio" name="star" id="star3" value="3">
                                <label for="star3"><i class="fas fa-star"></i></label>
                                <input type="radio" name="star" id="star2" value="2">
                                <label for="star2"><i class="fas fa-star"></i></label>
                                <input type="radio" name="star" id="star1" value="1">
                                <label for="star1"><i class="fas fa-star"></i></label>
                            </div>
                        </div>
                    </form>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
                    <button type="button" class="btn btn-primary" onclick="submitRating()">Submit</button>
                </div>
            </div>
        </div>
    </div>

    <div class="container image-cont">
        <div class="box-image">
            <% if (vendorWorks && Array.isArray(vendorWorks)) { %>
                <% vendorWorks.forEach(function(works) { %>
                    <% if (works.profileImage && works.profileImage.data) { %>
                        <p style="color: #b8860B; font-size: large; font-weight: 600;">Uploaded Date: <%= works && works.createdAt ? new Date(works.createdAt).toISOString().split('T')[0] : "No date" %></p>
                        <img src="data:<%= works.profileImage.contentType %>;base64,<%= Buffer.from(works.profileImage.data).toString('base64') %>" alt="Vendor Image" style="width: 400px; height: 400px;">
                    <% } else { %>
                        <p>No profile image available</p>
                    <% } %>
                <% }) %>
            <% } else if (vendorWorks && typeof vendorWorks === 'object') { %>
                <% if (vendorWorks.profileImage && vendorWorks.profileImage.data) { %>
                    <img src="data:<%= vendorWorks.profileImage.contentType %>;base64,<%= Buffer.from(vendorWorks.profileImage.data).toString('base64') %>" alt="Vendor Image" style="width: 400px; height: 400px;">
                <% } else { %>
                    <p>No profile image available</p>
                <% } %>
            <% } else { %>
                <p>No works found</p>
            <% } %>
        </div>
    </div>

    <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
    <script src="/bootstrap/js/bootstrap.min.js"></script>
    <script>
        // Show modal on page load or button click
        $(document).ready(function () {
            $('#ratingModal').modal('show');
        });

        function submitRating() {
            const rating = document.querySelector('input[name="star"]:checked').value;
      alert(`Thank you for your rating: ${rating} stars!`);
      // Here, you can send the rating to your server using AJAX or a form submission.
      $('#ratingModal').modal('hide');
        }
    </script>
</body>
</html>
